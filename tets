import { useSpinner } from './SpinnerContext';

export const setupMockUseSpinner = (showMock, hideMock) => {
  // Mock the useSpinner hook to return the provided mock functions
  jest.mock('./SpinnerContext', () => ({
    __esModule: true,
    useSpinner: () => ({
      show: showMock,
      hide: hideMock,
      message: '',
    }),
  }));
};
